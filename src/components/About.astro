---
import { about } from '../data/site';
---

<section class="section" id="about">
  <div class="section-head">
    <h2>About</h2>
  </div>
  <div class="prose">
    {about.map((p) => (
      <p>{p}</p>
    ))}
  </div>
</section>
